return Pages.Three.extend({
    /**
     * Extend from the same type as the widget. Add any properties you want to
     * allow the user to configure.
     * See the Property Pages documentation: https://squirro.atlassian.net/wiki/display/DOC/Custom+Widgets+API#CustomWidgetsAPI-PropertyPages
     */
    /*
    customNormal: [
        {
            view: Properties.MultilineText,
            config: {
                inputName: 'detailDashboard',
                infoText: 'Item detail dashboard',
                maxLines: 1
            }
        }
    ]
    */
});
